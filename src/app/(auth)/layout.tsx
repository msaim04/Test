/**
 * Auth layout for login and signup pages
 * Note: html and body tags are provided by the root layout
 */
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

